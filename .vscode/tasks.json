// .vscode/tasks.json - Korrigierte Version
{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell", // <--- Geändert zu "shell"
            "label": "C/C++: gcc build active file",
            "command": "/usr/bin/gcc -g '${file}' -o '${fileDirname}/${fileBasenameNoExtension}' ${env:CFLAGS}",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compiler: /usr/bin/gcc"
        }
    ]
}